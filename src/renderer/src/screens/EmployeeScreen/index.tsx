import React from 'react';
import EmployeeScreenNew from './EmployeeScreenNew';

// This is just a wrapper to import the clean EmployeeScreenNew component
const EmployeeScreen: React.FC = () => {
  return <EmployeeScreenNew />;
};

export default EmployeeScreen;
